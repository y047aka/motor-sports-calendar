{
  "scripts": {
    "start": "parcel src/index.html",
    "build": "rimraf docs && parcel build src/index.html --dist-dir docs --no-source-maps"
  },
  "devDependencies": {
    "elm": "^0.19.1-5",
    "elm-format": "^0.8.5",
    "elm-hot": "^1.1.6"
  },
  "dependencies": {
    "@parcel/transformer-elm": "^2.2.1",
    "@parcel/transformer-sass": "^2.2.1",
    "parcel": "^2.2.1",
    "rimraf": "^3.0.2"
  }
}
